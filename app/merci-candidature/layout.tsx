export const dynamic = 'force-dynamic';
export const dynamicParams = true;

export default function MerciCandidatureLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

